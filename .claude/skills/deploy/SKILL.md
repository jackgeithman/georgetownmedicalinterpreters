## Deploy to Vercel
1. Run `npm run build` to verify no build errors
2. Check that all required env vars are set in `.env.local`
3. Confirm the current branch is up to date with main
4. Push changes and report the Vercel deployment URL
