## Create PR
1. Run `npm run build` and fix any errors
2. Stage all changes with `git add -A`
3. Create a descriptive commit message based on the changes
4. Push to a new branch named after the feature
5. Create a PR using `gh pr create` with a summary of changes
